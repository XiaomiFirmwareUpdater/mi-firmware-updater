version: 2
jobs:
  build:
    steps:
      - checkout
      - run:
         name: install packages
         command: sudo apt-get install -y pv wput
      - run: chmod 775 script.sh
      - run: ./script.sh
